# Terraform state — never commit this, it contains secrets
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# Local vars — contains your server IP and paths
terraform.tfvars

# Crash logs
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
